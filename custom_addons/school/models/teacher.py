from odoo import models, fields

class Teacher(models.Model):
    # This is the name that will be used to name the table in the database
    _name = "school.teacher" # database table name "school_teacher"
    _description = "Teacher"

    # TODO : Add fields required fort his model to be functional


